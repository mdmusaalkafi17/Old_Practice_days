#include<stdio.h>

int main()
{
    int T;

    scanf("%d", &T);

    while(T--)
    {
        int n, i, result, first, last;

        scanf("%d", &n);

        last = n % 10;

        for(i = 1; i <= )

    }
}
